version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB_USER=usr_polimusic_md
      - DB_PASSWORD=Politecnica1
      - DB_NAME=BDD_PoliMusic_SongMD
      - DB_SERVER=sql-polimusic-server-md.database.windows.net
      - NODE_ENV=production
    restart: always